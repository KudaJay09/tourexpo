@AGENTS.md
inside the travel-preference-form, in the suggestions, if i select say a destination I want to be redirected directly
to the selected destination [id] page rather than the search. It would also be best if we removed the search
directory and any related code that redirects to it

